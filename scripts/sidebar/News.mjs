/**
 * News 模块子文件夹配置
 *
 * 日期文件夹（2026-09-04 等）不在此逐一配置：
 * gen-sidebar 会用日期目录名作组标题（字典序 = 时间序），
 * 图标用 defaultFolderIcon。
 *
 * 如需为某期单独指定标题/图标/排序，可在此添加，例如：
 * "2026-09-04": { title: "创刊号 · 2026-09-04", icon: "newspaper", order: 0 },
 *
 * @type {Record<string, { title?: string, icon?: string, order?: number }>}
 */
export default {};
