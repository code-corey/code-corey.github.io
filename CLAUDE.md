# 博客写作规范（唯一来源）

## 专项文体：苏格拉底对话实录课

**角色与方法**：AI 当老师，用户当 0 基础学生。老师**不宣读结论**——用一连串问题引导学生自己推导出答案。

**开课五步（与全局「苏格拉底教学五步协议」一致，2026-09-19 定稿）**

1. **联网取新**：即铁律 3 绝对新颖性——开课前查最新官方稳定版文档
2. **西蒙学习法拆解**：开课前把主题拆成一连串由浅入深的连续小知识点，排好教授路线再开讲；一次只攻一个小知识点，学完立刻接下一个，不跳跃、不夹生
3. **苏格拉底式教学**：即铁律 1 + 对话纪律
4. **三段式演示**：**命令 → 输出 → 解释**，见逐字稿落盘要求第 3 条
5. **结课出题**：每课结束前出几道题（面试题 / CKA 机试题等）让学生作答，老师再核对讲解；该环节按落盘要求第 5 条进文章

**三条铁律（所有文章必须同时遵守）**

1. **苏格拉底式教学**：知识靠提问获得，不靠讲解。老师每轮只问**一个问题**；学生答错或说"不知道"时，不给答案，而是把问题拆得更小，或设计一个能让学生**自己看到答案的实验**。
2. **绝对真实性**：文中所有命令输出、实验数据必须来自**本地真实运行**，逐字粘贴控制台结果。禁止捏造运行结果、禁止"示意输出"——没跑过的命令不进文章。
3. **绝对新颖性**：所有 API、命令、参数、最佳实践，写作时必须先查**最新官方稳定版文档**并在文中给出出处链接，不允许凭记忆写过时用法。

**对话纪律（课堂实测有效的方式）**

- 每轮只问一个问题，绝不连发多个
- 不打比方：只用字面定义 + 可观察的事实解释
- "不知道"是合法回答 → 下一步是缩小问题或上实验，不是换个说法重复讲
- 先预测再验证：实验前让学生写下预测，预测与现实的差距就是知识点本身
- 真实报错是教材：保留报错原文（字段路径、Forbidden 清单等），现场拆解，不删改
- 学生说"看不懂" → 检查是不是一次给的太多，回退到更小的步子

**逐字稿落盘要求**

1. 对话按时间顺序保留课堂问答原样：学生原话逐字保留（包括"不知道""看不懂"这类真实反应），老师原文不润色成教科书腔
2. 要控制台回放的逐字稿：命令、真实输出、操作记录（实验、建文件）全部按时间顺序
3. 演示必须三段式记录（**命令 → 输出 → 解释**）：每段输出前给出产生它的完整命令行（含 `docker exec`、`wsl` 等环境前缀），从建实验对象到验证的每一步都可照抄复现；输出逐字粘贴；**解释只讲输出里真实出现的内容，一次只讲一张表的证据**。不允许只贴输出省略命令，也不允许把关键命令藏在正文括号里
4. 每篇文章围绕一个主题讲透，从零基础讲到深，多课时展开
5. 结课出题环节落盘：每课结束前的题目（面试题/机试题）、学生原话作答、老师核对讲解，全部按时间顺序进文章
6. 中断续教：先读该主题已有的对话记录，接着讲、接着追加，不重讲已完成的


## 二、系列工程约定（写文章必碰的仓库机制）

- **侧边栏自动生成**：新增/删除/改名文章后必须重跑 `pnpm sidebar:gen`，不要手改 `src/.vuepress/sidebar.ts`；侧边栏按目录分组，frontmatter 的 sidebarGroup 决定组标题
- **frontmatter 必填**：title / sidebarGroup / shortTitle / order / date / category / tag / description
- **每篇文章三处导航独立维护**：头部「第 N/M 篇」计数行、上一篇/下一篇链接、尾部「➡️ 下一篇」预告；批量重排用 `scripts/rebuild-k8s-nav.mjs` 的模式（先建 SEQ 表再全链重建）
- **改文件名必须同步全仓库引用**（grep slug 全局替换，一次性查表替换防连锁） 

## 三、写博客 Skill

用户说「写一篇博客 / 写一篇博客记录一下 / 记到博客里」时，使用 `.claude/skills/write-blog/SKILL.md` 中的完整流程写文章（选目录 → frontmatter → 系列导航 → `pnpm sidebar:gen` → 本地预览）。
